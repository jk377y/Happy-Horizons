<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<title>{{ title }}</title>
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" />
		<link rel="stylesheet" href="/css/{{stylesheet}}" />
	</head>
	
    <body>

		<header>
			<div class="header-container">
				<div class="titleDiv">
					<h2 class="title">Happy Horizons</h2>
					<h2 class="title">Admin Panel</h2>
				</div>
				<nav class="navBar">
					<a class="navLink adminText" href="/">Home</a>
					<a class="navLink adminText" href="/navigate/contactUs">Contact Us</a>
					{{#if loggedIn}}
						<a class="navLink adminText" href="/navigate/myPortal">MyPortal</a>
						<a class="navLink adminText" href="/navigate/ticket">Open Ticket</a>
						{{#if isAdmin}}
							<a class="navLink adminText" href="/navigate/admin">Admin</a>
						{{/if}}
					{{/if}}
				</nav>
			</div>
		</header>

		{{#if loggedIn}}
			<section class="adminUserWrap">
				<form action="/logout" method="POST" class="userWindow">
					<h3 class="adminText">Logged in as <span class="user">{{firstName}}</span></h3>
					<div class="logoutSubmitBtnDiv">
						<input class="logoutSubmitBtn submitBtn" type="submit" value="Logout" />
					</div>
				</form>
			</section>
		{{/if}}

		<main>
			<div class="adminNavs">
				<a href="/navigate/admin/users" id="usersBtn" class="adminNavBtns">Users</a>
				<a href="/navigate/admin/units" id="unitsBtn" class="adminNavBtns">Units</a>
				<a href="/navigate/admin/tickets" id="ticketsBtn" class="adminNavBtns">Tickets</a>
				<a href="/navigate/admin/parking" id="parkingBtn" class="adminNavBtns">Parking</a>
				<a href="/navigate/admin/analytics" id="analyticsBtn" class="adminNavBtns">Analytics</a>				
			</div>
			{{{body}}}
		</main>

	</body>
</html>